paths:
    migrations: ../db/migrations
    seeds: ../db/seeds

environments:
    default_migration_table: migration
    default_database: development

    development:
        adapter: mysql
        host: localhost
        name: phonebook
        user: root
        pass: root
        port: 3306
        charset: utf8